import { defineFunction } from '@aws-amplify/backend';

export const sendBillReminders = defineFunction({
  name: 'send-bill-reminders',
  // You can add schedules here if needed, or trigger it manually
});
